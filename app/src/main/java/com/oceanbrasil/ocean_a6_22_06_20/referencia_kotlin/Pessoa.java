package com.oceanbrasil.ocean_a6_22_06_20.referencia_kotlin;/*
package com.oceanbrasil.ocean_a6_22_06_20;

import java.util.Objects;

public class Pessoa {
    private int id;
    private String nome;

    public Pessoa(int id, String nome) {
        this.id = id;
        this.nome = nome;
    }

    public int getId() {
        return id;
    }

    public String getNome() {
        return nome;
    }

    @Override
    public boolean equals(Object o) {
        if (this == o) return true;
        if (o == null || getClass() != o.getClass()) return false;
        Pessoa pessoa = (Pessoa) o;
        return id == pessoa.id &&
                Objects.equals(nome, pessoa.nome);
    }

    @Override
    public int hashCode() {
        return Objects.hash(id, nome);
    }

    @Override
    public String toString() {
        return "Pessoa{" +
                "id=" + id +
                ", nome='" + nome + '\'' +
                '}';
    }
}

class Testes {
    private void teste() {
        Pessoa paulo = new Pessoa(1, "Paulo");

        int id = paulo.getId();
        String nome = paulo.getNome();
    }
}
*/
